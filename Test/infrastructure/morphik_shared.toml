# ОБЩАЯ КОНФИГУРАЦИЯ ДЛЯ ВСЕХ ВЕРСИЙ
# Копируйте этот файл в каждую версию и меняйте только нужные параметры

# Ollama models - используют общий сервер
ollama_llama = { model_name = "ollama/llama3.2:3b", api_base = "http://morphik_ollama_shared:11434" }
ollama_embedding = { model_name = "ollama/nomic-embed-text", api_base = "http://morphik_ollama_shared:11434" }

# Все остальные настройки остаются теми же...