# Server connection settings
SERVER_IP=135.181.106.12

# PostgreSQL (прямое подключение)
POSTGRES_HOST=${SERVER_IP}
POSTGRES_PORT=5432
POSTGRES_USER=morphik
POSTGRES_PASSWORD=morphik
POSTGRES_DB=morphik

# Redis (прямое подключение)
REDIS_HOST=${SERVER_IP}
REDIS_PORT=6379

# Ollama (если доступен извне)
OLLAMA_HOST=${SERVER_IP}
OLLAMA_PORT=11434
OLLAMA_BASE_URL=http://${OLLAMA_HOST}:${OLLAMA_PORT}

# API URLs
REMOTE_API_URL=http://${SERVER_IP}:8000
REMOTE_UI_URL=http://${SERVER_IP}:3000

# Database URL for API
DATABASE_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}

# Redis URL for API
REDIS_URL=redis://${REDIS_HOST}:${REDIS_PORT}

# Local development ports
LOCAL_API_PORT=8001
LOCAL_UI_PORT=3001